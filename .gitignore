node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Auth / session state (e.g. agent-browser storage dumps)
auth.json
*.auth.json

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright
/test-results
/playwright-report
/playwright/.cache

# Audio files (served from GitHub Releases via oisincoveney/ll-audio)
language-transfer/

# Transcripts (served from GitHub via oisincoveney/ll-episodes)
transcripts/

# Database
data/*.db
data/*.db-shm
data/*.db-wal


# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
